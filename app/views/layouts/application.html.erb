<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Demo App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindplus/elements@1" type="module"></script>
    <script>
      window.algolia_search_api_key = '<%= ENV.fetch("ALGOLIA_SEARCH_API_KEY") %>'
      window.algolia_app_id         = '<%= ENV.fetch("ALGOLIA_APPLICATION_ID") %>'

      window.lead_index_name    = '<%= Lead.index_name %>'
      window.post_index_name = '<%= Post.index_name %>'
    </script>
  </head>

  <body data-controller="dialog">
    <%= render "layouts/sidebar" %>
    <div class="lg:pl-72">
      <main class="px-4 sm:px-6 lg:px-8 py-6">
        <%= yield %>
      </main>
    </div>
    <%= render "layouts/dialog" %>
  </body>
</html>
